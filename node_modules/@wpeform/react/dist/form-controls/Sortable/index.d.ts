import { ReactNode } from 'react';
import { CONTROLCLASS } from './styled';
export { CONTROLCLASS };
export declare type SortableItem = {
    id: string;
    label: ReactNode;
};
export declare type SortableProps = {
    /**
     * Current order of items. Every item in array must represent the id of
     * the actual item.
     */
    currentOrder: string[];
    /**
     * Callback function to update order.
     */
    updateOrder: (finalOrder: string[]) => void;
    /**
     * Sortable items.
     */
    items: SortableItem[];
    /**
     * Whether or not to lock drag vertically.
     *
     * @default false
     */
    lockVertically?: boolean;
    /**
     * Additional classNames.
     */
    className?: string | (string | undefined)[];
};
export default function Sortable(props: SortableProps): JSX.Element;
