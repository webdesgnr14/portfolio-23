import { GenericControlProps } from "../utils/types";
import { SettingsAppearanceControlAlignmentEnum } from "../../apollo/generated/types/graphql-global-types";
import { RatingItem } from './types';
export type { RatingItem };
export declare type RatingProps = {
    /**
     * Additional classNames.
     */
    className?: string | (string | undefined)[];
    /**
     * Currently selected value. It is the index of the item which is selected.
     */
    value: number;
    /**
     * Onchange handler. The passed newVal is the index of the item being
     * selected.
     */
    onChange: (newVal: number) => void;
    /**
     * Rating items, in the order they should appear.
     *
     * ```ts
     * type RatingItem = {
     * 	id: string;
     * 	icon: string;
     * }
     * ```
     */
    items: RatingItem[];
    /**
     * Alignment of the placeholder.
     *
     * @default LEFT
     */
    controlAlignment?: SettingsAppearanceControlAlignmentEnum;
} & Omit<GenericControlProps, 'readOnly'>;
export default function Rating(props: RatingProps): JSX.Element;
