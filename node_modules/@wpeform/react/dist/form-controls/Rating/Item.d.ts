import { GenericControlProps } from "../utils/types";
import { RatingItem } from './types';
export declare type ItemProps = {
    id: string;
    onChange: (newVal: number) => void;
    item: RatingItem;
    index: number;
    selected: number;
    hoverIndex: number | null;
    setHoverIndex: (index: number) => void;
} & Pick<GenericControlProps, 'name' | 'ariaLabel' | 'ariaLabelledby' | 'disabled'>;
export default function Item(props: ItemProps): JSX.Element;
