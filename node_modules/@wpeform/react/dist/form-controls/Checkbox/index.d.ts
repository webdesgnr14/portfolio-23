import React from 'react';
import { GenericControlProps } from "../utils/types";
export declare const CONTROLCLASS = "wpeform-control-checkbox";
export declare type CheckboxProps = Omit<GenericControlProps, 'readOnly' | 'hasError'> & {
    /**
     * Whether or not the checkbox is checked.
     */
    checked: boolean;
    /**
     * On Change handler. It passes the current state of the checkbox instead
     * of the event.
     */
    onChange: (nexValue: boolean) => void;
    /**
     * onBlur handler.
     */
    onBlur?: (event: React.FocusEvent<HTMLInputElement>) => void;
    /**
     * Fontawesome Icon class for the checkbox.
     *
     * @default "fas fa-check"
     */
    icon?: string;
    /**
     * The label HTML associated with the checkbox component.
     */
    text?: React.ReactNode;
};
/**
 * A controlled checkbox component with an associated label.
 */
export default function Checkbox(props: CheckboxProps): JSX.Element;
