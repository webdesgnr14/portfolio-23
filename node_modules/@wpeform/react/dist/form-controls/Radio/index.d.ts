import React from 'react';
import { GenericControlProps } from "../utils/types";
export declare const CONTROLCLASS = "wpeform-control-radio";
export declare type RadioProps = Omit<GenericControlProps, 'readOnly' | 'hasError'> & {
    /**
     * Whether or not the radio is checked.
     */
    checked: boolean;
    /**
     * On Change handler. It passes the current state of the radio instead
     * of the event.
     */
    onChange: (nexValue: boolean) => void;
    /**
     * On Blur event.
     */
    onBlur?: (event: React.FocusEvent<HTMLInputElement>) => void;
    /**
     * Fontawesome Icon class for the radio.
     *
     * @default "fas fa-circle"
     */
    icon?: string;
    /**
     * The label HTML associated with the radio component.
     */
    text?: React.ReactNode;
};
/**
 * A controlled radio component with an associated label.
 */
export default function Radio(props: RadioProps): JSX.Element;
