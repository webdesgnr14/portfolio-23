import { SubmissionWithForm } from "../../../../../core/submission/graphql/gql-types/SubmissionWithForm";
export declare type CancelEditHeaderProps = {
    /**
     * Callback to do when user clicks the cancel edit button for the submission.
     */
    onUserCancelEdit?: () => void;
    /**
     * Submission with Form GraphQL data.
     */
    submissionWithFormData: SubmissionWithForm;
};
export default function CancelEditHeader(props: CancelEditHeaderProps): JSX.Element;
