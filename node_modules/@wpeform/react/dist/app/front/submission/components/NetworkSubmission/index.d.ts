import { ThemeStyle } from "../../../components/FallbackContainer";
import { SubmissionEditProps } from '../SubmissionEdit';
import { SubmissionPreviewProps } from '../SubmissionPreview';
export declare type NetworkSubmissionProps = {
    mode: 'userpreview' | 'adminpreview' | 'useredit' | 'adminedit';
    id?: string;
    token?: string;
    /**
     * Styles as received from server side render.
     */
    themeStyle?: ThemeStyle | null;
    /**
     * Number of panels for skeleton.
     *
     * @default 2
     */
    panels?: number;
    /**
     * Number of controls for skeleton.
     *
     * @default 3
     */
    controls?: number;
    /**
     * Routine to run on after edit.
     */
    onAfterEdit?: () => void;
    /**
     * Callback to re-enter submission id.
     */
    onReEnterSubmissionId?: () => void;
    /**
     * Whether or not to put the entire UI in a shadow DOM.
     *
     * @default false
     */
    inShadow?: boolean;
    /**
     * CSS Handles to pass to Shadow component.
     *
     * @default []
     */
    cssHandles?: string[];
} & Pick<SubmissionPreviewProps, 'onUserEdit'> & Pick<SubmissionEditProps, 'onUserCancelEdit'>;
export default function NetworkSubmission(props: NetworkSubmissionProps): JSX.Element;
