import React from 'react';
import { SettingsAppearanceContainerLayoutEnum } from "../../../../apollo/generated/types/graphql-global-types";
import { ThemeStyle, useThemeStyleFromFormStyle, getThemeStyleFromFormStyle } from "../../../../components/ThemedContainer";
export type { ThemeStyle };
export { useThemeStyleFromFormStyle, getThemeStyleFromFormStyle };
export declare type FallbackContainerProps = {
    children: React.ReactNode;
    /**
     * Styles, as we have received from the server. This should match with the
     * ThemeStyle configuration.
     */
    themeStyle?: ThemeStyle | null;
    /**
     * Mode of inner ThemedContainer.
     *
     * @default live
     */
    mode?: 'live' | 'preview';
    /**
     * Override width of the inner ThemedContainer.
     */
    widthOverride?: string;
    /**
     * Override layout of the inner ThemedContainer.
     */
    containerLayoutOverride?: SettingsAppearanceContainerLayoutEnum;
    /**
     * Whether or not this fallback container will be server rendered.
     *
     * @default false
     */
    ssr?: boolean;
};
export default function FallbackContainer(props: FallbackContainerProps): JSX.Element;
