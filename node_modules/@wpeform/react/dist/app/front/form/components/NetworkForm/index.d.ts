import type { FormSubmissionProps } from "../../../../../core/form/form-submission";
import { ThemeStyle } from '../../../components/FallbackContainer';
export declare type NetworkFormProps = {
    id: string;
    /**
     * Styles as received from server side render.
     */
    themeStyle?: ThemeStyle | null;
    /**
     * Number of panels for skeleton.
     *
     * @default 2
     */
    panels?: number;
    /**
     * Number of controls for skeleton.
     *
     * @default 3
     */
    controls?: number;
    /**
     * Mode of the form.
     *
     * @default newsubmission
     */
    mode?: FormSubmissionProps['mode'];
    /**
     * Whether or not to put the entire UI in a shadow DOM.
     *
     * @default false
     */
    inShadow?: boolean;
    /**
     * CSS Handles to pass to Shadow component.
     *
     * @default []
     */
    cssHandles?: string[];
    /**
     * Whether or not we are going to do a SSR.
     *
     * @default false
     */
    ssr?: boolean;
    /**
     * Whether or not to increase view count in the form query.
     * Should be used only in headless mode.
     *
     * @default false
     */
    increaseViewCount?: boolean;
};
export default function NetworkForm(props: NetworkFormProps): JSX.Element;
