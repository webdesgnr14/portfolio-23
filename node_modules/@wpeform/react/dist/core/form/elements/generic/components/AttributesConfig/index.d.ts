import { ReactElement } from 'react';
import { OptionsList } from '../McqConfigs/hooks';
export declare type AttributesConfigProps = {
    defaultValues?: OptionsList;
    isNumeric?: boolean;
    isMultiline?: boolean;
    defaultValueDescription?: string;
    /**
     * How to render the value comparison.
     */
    renderValue?: (value: string, setValue: (newValue: string) => void) => ReactElement;
    /**
     * Prefil type.
     *
     * @default 'all'
     */
    prefilType?: 'none' | 'all';
};
export default function AttributesConfig({ defaultValues, isNumeric, isMultiline, defaultValueDescription, renderValue, prefilType, }: AttributesConfigProps): JSX.Element;
