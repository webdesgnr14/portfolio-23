import { FormElementInput, FormStructureInput } from "../../../../../apollo/generated/types/graphql-global-types";
import { FormReducerStateType, FormReducerInputType } from '../types';
export declare function setMainStates(draftState: FormReducerStateType, state: FormReducerInputType): void;
export declare function pushFormInputStateToStorage(storage: FormReducerInputType[], state: FormReducerInputType, placement: 'beginning' | 'end'): void;
/**
 * Create a deep duplicate of elements and adds the element to
 * `draftState.elements`.
 *
 * It also copies element children if any and add them to the
 * `draftState.elements`.
 *
 * @param draftState Current state from immer (immutable)
 * @param element The element to duplicate.
 * @returns Id of the duplicated element.
 */
export declare function getElementAfterDuplicate(draftState: FormReducerInputType, element: FormElementInput): string | false;
export declare function getNewEmptyStructure(id: string, title: string): FormStructureInput;
