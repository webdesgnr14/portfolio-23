import type { AdminElementCollectionItemType } from "../../../../collections/elements/types";
export declare type ElementsTitleMapType = {
    [id: string]: string;
};
/**
 * Get a memoized list of element id => title map. Useful in mentions
 * List etc.
 *
 * @param excludeId Id or Ids of specific elements to exclude in the list.
 * @param excludeTypes Types of items to exclude in the list. By default design
 *                     and security elements are excluded.
 */
export declare function useFormDataElementsTitleMap(excludeId?: string | string[], excludeTypes?: AdminElementCollectionItemType['type'][]): ElementsTitleMapType;
