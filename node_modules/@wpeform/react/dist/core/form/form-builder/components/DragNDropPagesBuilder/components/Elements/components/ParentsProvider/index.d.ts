import React from 'react';
export declare type ParentType = {
    type: 'structure' | 'element' | 'pool';
    id: string;
};
export declare function useElementParents(): ParentType[];
export default function ParentsProvider(props: ParentType & {
    children: React.ReactNode;
}): JSX.Element;
