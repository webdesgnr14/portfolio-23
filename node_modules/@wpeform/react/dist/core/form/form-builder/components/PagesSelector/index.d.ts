import { SelectProps } from 'formik-antd';
export declare type PagesSelectorProps = SelectProps & {
    placeholder?: string;
    name: string;
};
export default function PagesSelector(props: PagesSelectorProps): JSX.Element;
