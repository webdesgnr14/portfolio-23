import React from 'react';
import { ApolloError } from '@apollo/client';
import { submissionReducerStateType, submissionStoreApiType } from '../../state/types';
import { FormForSubmission_form } from '../../graphql/gql-types/FormForSubmission';
import { FSubmissionData } from '../../graphql/gql-types/FSubmissionData';
export declare type FormProps = {
    /**
     * Initial store data. Use this to pre-render form with everything. Useful
     * when storing state to JSON/localStorage or showing existing submission
     * etc.
     *
     * If this is not given, then a default one will be used.
     */
    initialData?: submissionReducerStateType;
    /**
     * Use this to externally set activePage.
     */
    activePage?: string | null;
    /**
     * Use this to sync back active page to parent.
     */
    onActivePageChange?: React.Dispatch<React.SetStateAction<string | null>>;
    /**
     * Whether or not the form is submitting.
     */
    submitting: boolean;
    /**
     * Callback for onSubmit.
     */
    onSubmit: (store: submissionStoreApiType) => void;
    /**
     * Whether or not the form is submitted.
     */
    submitted: boolean;
    /**
     * State after submission.
     */
    submittedState: 'success' | 'error' | 'networkerror';
    /**
     * Submission Error for networkerror.
     */
    submissionError?: ApolloError;
    /**
     * Callback for on reset.
     */
    onReset?: () => void;
    /**
     * Callback for retry on submission error.
     */
    onErrorRetry?: () => void;
    /**
     * Pre-emptive Limitations to show on the form.
     */
    limitations?: FormForSubmission_form['limitations'];
    /**
     * Notices to show on the form.
     */
    notices?: FormForSubmission_form['notices'];
    /**
     * Mutation data as received from GraphQL. We use it for after submission
     * mentions and dynamic anchor tags.
     */
    mutationData?: FSubmissionData;
};
export default function Form(props: FormProps): JSX.Element;
