import { ReactNode } from 'react';
import { FormProps } from './components/Form';
import { submissionFormDataContextType, submissionFormMetaContextType, submissionFormRenderModeContextType } from './contexts/data';
export declare type FormSubmissionProps = {
    /**
     * Form data to render the form skeleton
     */
    formData: submissionFormDataContextType;
    /**
     * Form Meta needed to create initial submissions data.
     */
    formMeta: submissionFormMetaContextType;
    /**
     * Rendering mode.
     */
    mode: submissionFormRenderModeContextType;
    /**
     * Additional form header. This is rendered inside a themed container.
     */
    formHeader?: ReactNode;
} & FormProps;
export default function FormSubmission(props: FormSubmissionProps): JSX.Element;
