import { Collections } from '..';
export declare type FontConfig = {
    name: string;
    fontFamily: string;
    enqueue: string | null;
};
export declare const wpEFormFonts: Collections<FontConfig, FontConfig>;
/**
 * Get font family and enqueue from an item in the collection. It safely
 * falls back to inherit and null if the item isn't present.
 *
 * @param item The item from the collection.
 */
export declare function getFontFamilyAndEnqueue(item: string): [string, string | null];
