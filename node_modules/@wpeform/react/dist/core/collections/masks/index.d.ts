import type { AcceptedMaskType } from 'react-imask';
import type { MaskedDynamic } from 'imask';
import { Collections } from '..';
export declare type DynamicMaskType = MaskedDynamic['mask'];
export declare type MaskType = AcceptedMaskType;
export declare type MasksConfig = {
    name: string;
    mask: MaskType;
    placeholder?: string;
    overrideInputType?: string;
    title?: string;
};
export declare const wpEFormMasks: Collections<MasksConfig, MasksConfig>;
