/// <reference types="react" />
import { FormSubmissionElementInput } from "../../apollo/generated/types/graphql-global-types";
import { submissionFormDataElementType } from "../form/form-submission/contexts/data";
/**
 * Safely get an element's answer value for rendering.
 *
 * @param element Element data from form.
 * @param value Submission data of the element (just the value).
 */
export declare function getElementAnswer(element: submissionFormDataElementType, value: FormSubmissionElementInput['value']): false | React.ReactNode;
/**
 * Safely get an element's display value for rendering.
 *
 * @param element Element data from form.
 * @param value Submission data of the element (just the value).
 */
export declare function getElementDisplayValue(element: submissionFormDataElementType, value: FormSubmissionElementInput['value']): false | React.ReactNode;
