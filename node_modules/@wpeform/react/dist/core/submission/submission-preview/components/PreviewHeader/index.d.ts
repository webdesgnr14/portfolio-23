export declare type PreviewHeaderProps = {
    mode: 'user' | 'admin';
    canUserEdit: boolean;
    /**
     * Callback to do when user clicks on the edit button for the submission.
     */
    onUserEdit?: () => void;
    printLink?: string | null;
    downloadLink?: string | null;
};
export default function PreviewHeader({ mode, canUserEdit, onUserEdit, printLink, downloadLink, }: PreviewHeaderProps): JSX.Element;
