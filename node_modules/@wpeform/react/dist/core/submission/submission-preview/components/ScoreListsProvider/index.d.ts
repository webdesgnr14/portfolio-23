import React, { PropsWithChildren } from 'react';
export declare type ScoreList = {
    title: React.ReactNode;
    description?: React.ReactNode;
    scoreId: string;
};
export declare type scoreListsContextType = {
    scores: ScoreList[];
    precision: number;
};
export declare function useScoreLists(): scoreListsContextType;
export default function ScoreListsProvider(props: PropsWithChildren<{}>): JSX.Element;
