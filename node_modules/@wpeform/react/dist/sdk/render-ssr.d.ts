import { PropsWithChildren } from 'react';
import { ThemeStyle } from "../app/front/components/FallbackContainer";
export declare type RenderSsrProps = {
    /**
     * Styles as received from server side render.
     */
    themeStyle?: ThemeStyle | null;
    /**
     * Number of panels for skeleton.
     *
     * @default 2
     */
    panels?: number;
    /**
     * Number of controls for skeleton.
     *
     * @default 3
     */
    controls?: number;
    /**
     * Whether or not to render the form in shadow dom, preventing conflict
     * with your application.
     *
     * @default false
     */
    inShadow?: boolean;
};
/**
 * Render with SSR support. If doing SSR, then show a skeleton, otherwise
 * render the real thing, with a suspense, so that it can be lazy imported.
 * The component tree is also wrapped inside a error boundary.
 *
 * @param props Component props.
 */
export declare function RenderSsr(props: PropsWithChildren<RenderSsrProps>): JSX.Element;
export { setupWPEFormGlobals, parseStyleData } from './setup';
export * from "../apollo/generated/types/graphql-global-types";
