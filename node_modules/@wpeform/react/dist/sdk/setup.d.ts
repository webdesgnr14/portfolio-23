import { ThemeStyle } from "../components/ThemedContainer";
export declare function setupWPEFormGlobals(globals: typeof WPEFormGraphQLApp): void;
/**
 * Parse styleData as retrieved from `forms` query.
 *
 * @param styleData StyleData as received from forms query.
 * @returns Parsed value.
 */
export declare function parseStyleData(styleData: string): {
    themeStyle: ThemeStyle;
    controls: number;
    panels: number;
};
