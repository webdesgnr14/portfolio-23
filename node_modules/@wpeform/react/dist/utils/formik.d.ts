export declare function AutoSave(): null;
/**
 * A utility to quickly get "name" for Formik Fields.
 *
 * @param prefix Common prefix for name.
 */
export declare function getFormikNamer<T extends object>(prefix: string): (name: Exclude<keyof T, '__typename'>, suffix?: string | undefined) => string;
