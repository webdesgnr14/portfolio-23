/**
 * Main Return type of parseMediaUrl function.
 */
export interface ParsedMediaUrl {
    filename: [string, string];
    cleanUrl: string;
    type: 'invalid' | 'video' | 'audio' | 'image' | 'youtube' | 'vimeo' | 'dailymotion' | 'wistia';
}
export declare const validImageExtensions: string[];
export declare const validVideoExtensions: string[];
export declare const validAudioExtensions: string[];
/**
 * Get dailymotion embed URL from user input.
 *
 * @param dirtyUrl URL from user input
 */
export declare function getDailyMotion(dirtyUrl: string): string;
/**
 * Get wistia embed URL from user input.
 *
 * @param dirtyUrl URL from user input.
 */
export declare function getWistia(dirtyUrl: string): string;
export declare type embedUrlParams = {
    includeModestYouTubeBranding: boolean;
};
export interface ParsedEmbedUrl {
    type: 'youtube' | 'vimeo' | 'dailymotion' | 'wistia' | 'invalid';
    cleanUrl: string;
}
/**
 * Parse possible embed URL from user input.
 *
 * @param url URL from user input.
 * @param params Options.
 */
export declare function parseEmbedUrl(url: string, params?: embedUrlParams): ParsedEmbedUrl;
export declare function parseMediaUrl(url: string, params?: embedUrlParams): ParsedMediaUrl;
