import * as React from 'react';
export declare type HighlightFuzzySearchProps = {
    /**
     * The query based on which we are to highlight the text.
     */
    query: string;
    /**
     * The text to highlight.
     */
    text: string;
    /**
     * The component or HTML tag using which we highlight the part.
     * @default strong.
     */
    tag?: React.ComponentType<{
        children: React.ReactNode | string;
    }> | keyof React.ReactHTML;
};
/**
 * Highlight matched portion from a fuzzy search. You can provide which
 * component to be used for highlighting, which give you most fine-grained
 * control of the highlight.
 *
 * Use with a library like [fuzzysearch](https://github.com/bevacqua/fuzzysearch).
 */
export default function HighlightFuzzySearch(props: HighlightFuzzySearchProps): JSX.Element;
