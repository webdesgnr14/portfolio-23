import { ReactNode } from 'react';
export declare function useIsShadow(): boolean;
export declare type ShadowProps = {
    /**
     * Whether or not we render the children in shadow root.
     */
    inShadow: boolean;
    /**
     * Children nodes to render.
     */
    children: ReactNode;
    /**
     * CSS handles (WP Specific) to extract from the main page and put them in
     * the shadow root.
     */
    cssHandles?: string[];
    /**
     * Whether or not this would be for server side rendering.
     *
     * @default false
     */
    ssr?: boolean;
};
declare function Shadow(props: ShadowProps): JSX.Element;
declare namespace Shadow {
    var LinkAllAssets: (props: {
        head?: boolean | undefined;
        body?: boolean | undefined;
    }) => JSX.Element;
}
export default Shadow;
