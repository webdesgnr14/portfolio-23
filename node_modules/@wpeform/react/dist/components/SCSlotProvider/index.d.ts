import { PropsWithChildren } from 'react';
export declare function useScSlot(): HTMLElement | undefined;
/**
 * A Slot provider component, to be used by the Renderer to keep styles
 * enclosed within the passed slot.
 *
 * The ThemedContainer works fine for SSR, but does not work reliably with
 * ref and StyleSheetManager target. So we better get the slot from DOM directly
 * and mark it as the stylesheetmanager target.
 *
 * @see https://github.com/styled-components/styled-components/issues/3548
 * @param props Component props.
 */
export default function ScSlotProvider(props: PropsWithChildren<{
    slot: HTMLElement | undefined;
}>): JSX.Element;
