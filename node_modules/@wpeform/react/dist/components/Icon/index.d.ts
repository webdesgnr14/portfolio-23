import React from 'react';
export declare const CONTROLCLASS = "wpeform-icon";
export declare type IconProps = {
    /**
     * Classes to be applied to the icon. Works for icon fonts like fontawesome
     * icomoon etc.
     *
     * @default undefined
     */
    iconClass?: string;
    /**
     * SVG (or any other) children to render directly inside the icon container.
     *
     * @default null
     */
    children?: React.ReactNode;
    component?: React.ComponentType<any>;
    /**
     * Aria label, to decorate the text.
     */
    ['aria-label']?: string;
    /**
     * Additional classnames.
     */
    className?: string;
    /**
     * Title.
     */
    title?: string;
};
/**
 * An icon component to safely render iconfont icons or SVG icons inline
 * within the component. Other components can modify the styling by overriding
 * `wpeform-icon` class inside them. It inherits color and size directly from
 * the parent component.
 */
export default function Icon(props: IconProps): JSX.Element;
