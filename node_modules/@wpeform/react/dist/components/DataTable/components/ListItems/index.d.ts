import React from 'react';
import { UseDataTableReturnTouple } from "../../utils";
export declare type ListItemsProps<ITEM, FILTER extends Record<string, any>> = {
    state: UseDataTableReturnTouple<FILTER>[0];
    dispatch: UseDataTableReturnTouple<FILTER>[1];
    totalItems: number;
    /**
     * Items to render in the datatable.
     */
    items?: ITEM[];
    /**
     * Callback to get item Id. The return should be unique.
     */
    getItemId: (item: ITEM) => string;
    /**
     * Callback to generate item row.
     */
    generateItemRow: (item: ITEM) => React.ReactNode;
    /**
     * Item name in singular.
     */
    itemNameSingular: string;
    /**
     * Item name in plural.
     */
    itemNamePlural: string;
    /**
     * Whether or not data is being loaded.
     */
    loadingData: boolean;
    /**
     * Empty text to show when item length is 0.
     */
    emptyText: React.ReactNode;
};
export default function ListItems<ITEM, FILTER extends Record<string, any>>(props: ListItemsProps<ITEM, FILTER>): JSX.Element;
