import { UseDataTableReturnTouple } from "../../../../utils";
export declare type ItemsInPageProps<ITEM, FILTER extends Record<string, any>> = {
    items?: ITEM[];
    itemNameSingular: string;
    itemNamePlural: string;
    state: UseDataTableReturnTouple<FILTER>[0];
    totalItems: number;
    loadingData: boolean;
};
export default function ItemsInPage<ITEM, FILTER extends Record<string, any>>(props: ItemsInPageProps<ITEM, FILTER>): JSX.Element;
