import React from 'react';
export declare const CONTROLCLASS = "wpeform-component-animatein";
export declare type AnimateInProps = {
    /**
     * Children of AnimateIn component.
     */
    children: React.ReactNode;
    /**
     * Mode of visibility. If in Preview mode, then depending on the visible prop
     * it will be faded. But in live mode, dependending on the visible prop, it
     * will be hidden.
     */
    mode?: 'live' | 'preview';
};
/**
 * An AnimateIn component to declaratively animate a component in when it
 * comes within the viewport. It uses IntersectionObserver under the hood.
 *
 * It animates the children components only once when offset from the bottom
 * is 50px.
 */
export default function AnimateIn(props: AnimateInProps): JSX.Element;
