import React from 'react';
import { Node } from 'slate';
import { MentionsProvider, useMentionsData, MentionsRendererProps } from "../../app/admin/components/RichEditor/plugins/mentions/renderer";
import { CONTROLCLASS } from './styled';
export { CONTROLCLASS };
export { MentionsProvider, useMentionsData };
declare type dynamicAnchorContextType = {
    [x: string]: string;
};
declare function DynamicAnchorProvider(props: {
    dynamicAnchors: dynamicAnchorContextType;
    children: React.ReactNode;
}): JSX.Element;
export declare type SlateViewProps = {
    /**
     * Nodes from the RichEditor component. Directly pass the value which is
     * saved from the `RichEditor` component. If you have saved in stringified
     * JSON, you will need to `JSON.parse` before using the component. Better use
     * `getNodesFromJSON` for a proper fallback.
     */
    nodes: Node[];
    /**
     * The mode of view. If it is singleline, then only the first node is
     * rendered.
     */
    mode: 'singleline' | 'multiline';
    /**
     * Exclude mentions characters from rendering. This is to prevent any
     * infinite recursion.
     */
    excludeMentionsCharacters?: string[];
    /**
     * Whether or not to render mentions.
     *
     * @default true
     */
    renderMentions?: boolean;
    /**
     * Additional className for the SlateView container.
     */
    className?: string;
    /**
     * Additional set of mentions, not covered by the primary handler.
     */
    additionalMentions?: MentionsRendererProps['additionalMentions'];
    /**
     * Additional set of dynamicAnchorTags, not covered by the primary handler.
     */
    additionalDynamicAnchorTags?: {
        [x: string]: string;
    };
};
/**
 * A component to that takes in SlateJS Nodes and renders corresponding
 * ReactNodes that our application understands. Pass saved values of all
 * RichEditor controls through this component and it will show up properly in
 * the application.
 */
export default function SlateView(props: SlateViewProps): JSX.Element | null;
export { isNodesEmpty, getNodesFromJSON, getStringValueFromNodes, } from "../../utils/slate";
export { DynamicAnchorProvider };
export * from './helpers';
