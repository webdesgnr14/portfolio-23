export declare const CONTROLCLASS = "wpeform-component-progressbar";
export declare type ProgressbarProps = {
    /**
     * Progress bar width (in percentage).
     */
    width: number;
    /**
     * Whether or not the progress bar is active.
     *
     * @default false
     */
    active?: boolean;
    /**
     * Whether or not the bottom part is rounded.
     *
     * @default false
     */
    isBottomRounded?: boolean;
    /**
     * Whether or not the top part is rounded.
     *
     * @default false
     */
    isTopRounded?: boolean;
    /**
     * Whether or not to show the value label.
     *
     * @default true
     */
    showLabel?: boolean;
    /**
     * Decimal precision to show for the progress.
     *
     * @default 2
     */
    precision?: number;
    /**
     * Position of label.
     *
     * @default top
     */
    labelPosition?: 'top' | 'bottom';
};
export default function Progressbar(props: ProgressbarProps): JSX.Element;
