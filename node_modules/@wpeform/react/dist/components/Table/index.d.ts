import { ReactNode } from 'react';
export declare type TableProps = {
    /**
     * Table children.
     */
    children: ReactNode;
    /**
     * Additional classNames.
     */
    className?: string | (string | undefined)[];
    /**
     * Type of table design.
     *
     * @default stripped
     */
    type?: 'stripped' | 'regular';
    /**
     * Whether or not highlight row on hover.
     *
     * @default true
     */
    highlightOnHover?: boolean;
    /**
     * Whether or not `table-layout` property should be `fixed`.
     *
     * @default false
     */
    fixedLayout?: boolean;
    /**
     * Whether or not the body is centered.
     *
     * @default false
     */
    isBodyCentered?: boolean;
};
/**
 * A generic table component for printing styled tables.
 *
 * @param props Table props.
 */
export default function Table(props: TableProps): JSX.Element;
