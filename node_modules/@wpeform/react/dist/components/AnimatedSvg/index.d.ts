export declare const CONTROLCLASS = "wpeform-component-animatedsvg";
export declare const CONTROLSVGCLASS: string;
export declare type AnimatedSvgProps = {
    /**
     * Whether or not the SVGs are printed on top of a colorful background. If
     * this is set to true, then the stroke is of light color and background is of
     * dark color.
     */
    colorfulBg: boolean;
    /**
     * Type of the SVG.
     */
    type: 'error' | 'warning' | 'success' | 'info';
    /**
     * Additional classNames.
     */
    className?: string;
};
/**
 * Animated SVG for feedback and other attention purpose.
 */
export default function AnimatedSvg(props: AnimatedSvgProps): JSX.Element;
