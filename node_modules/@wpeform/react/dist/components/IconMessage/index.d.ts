import * as React from 'react';
declare type IconMessageProps = {
    /**
     * The icon to render. Instead of having some hard control, you pass in
     * something React can render, like SVG or `<i className="..." />` etc.
     */
    icon: React.ReactNode;
    /**
     * The main message.
     */
    children: React.ReactNode;
};
export default function IconMessage({ icon, children }: IconMessageProps): JSX.Element;
export {};
