import React from 'react';
import { GenericAlignmentEnum } from "../../apollo/generated/types/graphql-global-types";
export declare const CONTROLCLASS = "wpeform-component-buttongroup";
export declare type ButtonsGroupProps = {
    /**
     * ButtonsGroup children. Should be `Components/Button`.
     */
    children: React.ReactNode;
    /**
     * Type of button group. Only `flat` buttons are supported in `navbar` mode.
     */
    type: 'regular' | 'navbar';
    /**
     * Whether or not buttons inside are supposed to be colorful. Useful only for
     * `navbar` mode and it does **NOT** alter the props of the children.
     *
     * @default false
     */
    colorful?: boolean;
    /**
     * Alignment of the children buttons.
     */
    align: GenericAlignmentEnum;
    /**
     * Additional className to apply to the group.
     */
    className?: string;
    /**
     * Whether or not has padding on regular view.
     *
     * @default false
     */
    hasGutterOnregular?: boolean;
};
/**
 * A ButtonsGroup component to group and align multiple buttons.
 */
export default function ButtonsGroup(props: ButtonsGroupProps): JSX.Element;
