import React from 'react';
export declare const CONTROLCLASS = "wpeform-component-alert";
export declare type AlertProps = {
    /**
     * Alert message as children.
     */
    children: React.ReactNode;
    /**
     * Alert Title.
     */
    title?: React.ReactNode;
    /**
     * Icon to show for the alert message.
     */
    icon?: string;
    /**
     * Whether or not the alert is dismissable.
     */
    dismissable?: boolean;
    /**
     * Type of the alert.
     */
    type: 'regular' | 'error' | 'warn' | 'success' | 'primary';
    /**
     * Additional className.
     */
    className?: string;
    /**
     * Callback when dismiss is called.
     */
    onDismiss?: () => void;
};
export default function Alert(props: AlertProps): JSX.Element;
