import React from 'react';
export declare const CONTROLCLASS = "wpeform-control-addon";
export declare type ControlAddonProps = {
    /**
     * Addon children. These should be composed from the parent.
     */
    children: React.ReactNode;
    /**
     * Whether or not the add-ons are visible.
     */
    visible: boolean;
    /**
     * Whether or not to provide top gutter.
     * @default true
     */
    gutterTop?: boolean;
    /**
     * Whether or not to provide top gutter.
     * @default false
     */
    gutterBottom?: boolean;
    /**
     * Whether or not to provide horizontal gutter.
     * @default false
     */
    gutterHorizontal?: boolean;
    /**
     * Additional class names.
     */
    className?: string;
};
/**
 * Use this component to animate in or out some add-on based on some logic.
 * Useful for things like Free Input inside some MCQ etc.
 */
export default function ControlAddon(props: ControlAddonProps): JSX.Element;
